import React, { Component } from 'react';

class Bs extends Component {
  render() {
    return (
      <div>
        LOGGED In
        // localStorage.getItem()
        // localStorage.removeItem("token")
        // clear local storage
        // sets hastoken to false and take back to landing page
        // on mount hit events endpoint
      </div>
    )
  }
}

export default Bs;
